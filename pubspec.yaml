name: kickoff
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1            # For API calls
  shared_preferences: ^2.2.2 # For local storage
  provider: ^6.0.2        # Add provider package for state management

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add assets if needed in the future
  # assets:
  #   - assets/images/
